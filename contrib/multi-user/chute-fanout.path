[Unit]
Description=Watch the shared Taildrop folder for new arrivals

[Path]
# Rewritten by install-user.sh if the shared folder is somewhere else.
PathChanged=/home/taildrop
Unit=chute-fanout.service

[Install]
WantedBy=default.target
